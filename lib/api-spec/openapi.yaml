openapi: 3.1.0
info:
  title: Api
  version: 0.1.0
  description: Multi-tenant video platform API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: onboarding
  - name: dashboard
  - name: videos
  - name: folders
  - name: workspace
  - name: custom-domain
  - name: members
  - name: billing
  - name: audit
paths:
  /onboarding:
    get:
      operationId: getOnboarding
      tags: [onboarding]
      responses:
        "200":
          description: Authenticated user's provider-neutral workspace onboarding state
          content: { application/json: { schema: { $ref: "#/components/schemas/WorkspaceOnboarding" } } }
        "401": { description: Authentication required }
  /onboarding/workspaces:
    post:
      operationId: createOnboardingWorkspace
      tags: [onboarding]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/WorkspaceOnboardingInput" } } }
      responses:
        "202":
          description: Workspace creation and background provisioning accepted
          content: { application/json: { schema: { $ref: "#/components/schemas/WorkspaceOnboarding" } } }
        "400": { description: Invalid workspace name or slug }
        "401": { description: Authentication required }
        "409": { description: User already belongs to a workspace or slug is unavailable }
  /onboarding/retry:
    post:
      operationId: retryOnboarding
      tags: [onboarding]
      requestBody:
        required: false
        content: { application/json: { schema: { $ref: "#/components/schemas/WorkspaceOnboardingRef" } } }
      responses:
        "202":
          description: Safe provisioning retry accepted
          content: { application/json: { schema: { $ref: "#/components/schemas/WorkspaceOnboarding" } } }
        "401": { description: Authentication required }
        "403": { description: Only the workspace owner may retry }
        "409": { description: Workspace state is not safely retryable }
  /billing/catalog:
    get:
      operationId: getBillingCatalog
      tags: [billing]
      responses:
        "200":
          description: Authoritative active billing catalog
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingCatalog" } } }
  /billing/subscription:
    get:
      operationId: getBillingSubscription
      tags: [billing]
      responses:
        "200":
          description: Current tenant billing projection
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingSubscription" } } }
  /billing/checkout:
    post:
      operationId: createBillingCheckout
      tags: [billing]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BillingPlanInput" } } }
      responses:
        "201":
          description: Hosted Stripe Checkout session
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingUrl" } } }
  /billing/change-plan:
    post:
      operationId: changeBillingPlan
      tags: [billing]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BillingPlanInput" } } }
      responses:
        "200":
          description: Immediate upgrade or scheduled downgrade
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingChangeResult" } } }
  /billing/cancel:
    post:
      operationId: cancelBillingSubscription
      tags: [billing]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BillingActionInput" } } }
      responses:
        "200":
          description: Cancellation scheduled at period end
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingCancellation" } } }
  /billing/resume:
    post:
      operationId: resumeBillingSubscription
      tags: [billing]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BillingActionInput" } } }
      responses:
        "200":
          description: Period-end cancellation removed
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingCancellation" } } }
  /billing/portal:
    post:
      operationId: createBillingPortal
      tags: [billing]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BillingActionInput" } } }
      responses:
        "200":
          description: Hosted Stripe customer portal
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingUrl" } } }
  /billing/invoices:
    get:
      operationId: listBillingInvoices
      tags: [billing]
      parameters:
        - { in: query, name: limit, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
        - { in: query, name: cursor, schema: { type: string } }
      responses:
        "200":
          description: Tenant customer invoices
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingInvoiceList" } } }
  /billing/reconcile:
    post:
      operationId: reconcileBillingSubscription
      tags: [billing]
      requestBody:
        required: true
        content: { application/json: { schema: { $ref: "#/components/schemas/BillingActionInput" } } }
      responses:
        "200":
          description: Reconciled billing status
          content: { application/json: { schema: { $ref: "#/components/schemas/BillingReconcileResult" } } }
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /public/runtime-config:
    get:
      operationId: getRuntimeConfig
      tags: [health]
      responses:
        "200":
          description: Public runtime display configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RuntimeConfig"
  /workspace:
    get:
      operationId: getWorkspace
      tags: [workspace]
      responses:
        "200":
          description: Current tenant workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
    patch:
      operationId: updateWorkspace
      tags: [workspace]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WorkspaceUpdate"
      responses:
        "200":
          description: Updated workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Workspace"
  /custom-domain:
    get:
      operationId: getCustomDomain
      tags: [custom-domain]
      responses:
        "200":
          description: Current workspace custom-domain ownership verification status.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomDomainStatus" }
    post:
      operationId: createCustomDomain
      tags: [custom-domain]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomDomainInput" }
      responses:
        "202":
          description: Domain ownership challenge created or replaced.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomDomainStatus" }
        "400": { description: Invalid or unsafe hostname }
        "403": { description: Workspace permission or entitlement required }
        "409": { description: Hostname is claimed by another workspace }
    delete:
      operationId: deleteCustomDomain
      tags: [custom-domain]
      responses:
        "204": { description: Local custom-domain ownership representation removed }
        "403": { description: Workspace permission or entitlement required }
  /custom-domain/verify:
    post:
      operationId: verifyCustomDomain
      tags: [custom-domain]
      responses:
        "202":
          description: Background DNS TXT ownership verification accepted.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomDomainStatus" }
        "403": { description: Workspace permission or entitlement required }
        "409": { description: No retryable custom-domain verification is pending }
        "429":
          description: Verification request rate limited.
          headers:
            Retry-After:
              schema: { type: integer, minimum: 1 }
  /dashboard:
    get:
      operationId: getDashboard
      tags: [dashboard]
      responses:
        "200":
          description: Workspace dashboard
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Dashboard"
  /videos:
    get:
      operationId: listVideos
      tags: [videos]
      parameters:
        - in: query
          name: search
          schema:
            type: string
            maxLength: 500
        - in: query
          name: status
          schema:
            type: string
            enum: [created, uploading, processing, ready, error]
        - in: query
          name: visibility
          schema:
            type: string
            enum: [private, unlisted, public]
        - in: query
          name: folderId
          schema:
            type: string
            description: A folder UUID, or root for videos with no folder.
        - in: query
          name: sort
          schema:
            type: string
            enum: [newest, oldest, title_asc, title_desc, plays_desc]
            default: newest
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 24
        - in: query
          name: cursor
          schema:
            type: string
            minLength: 1
            maxLength: 2048
      responses:
        "200":
          description: Tenant video library
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VideoList"
  /videos/bulk:
    patch:
      operationId: bulkUpdateVideos
      tags: [videos]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkVideoUpdate"
      responses:
        "200":
          description: Ordered bulk video update result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkVideoActionResult"
  /videos/bulk-delete:
    post:
      operationId: bulkDeleteVideos
      tags: [videos]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkVideoDeleteInput"
      responses:
        "200":
          description: Ordered bulk video deletion result, including partial failures
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkVideoActionResult"
  /folders:
    get:
      operationId: listFolders
      tags: [folders]
      parameters:
        - in: query
          name: parentId
          required: true
          schema:
            type: string
            description: A folder UUID, or root for top-level folders.
      responses:
        "200":
          description: Direct child folders in the requested location
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Folder"
    post:
      operationId: createFolder
      tags: [folders]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FolderInput"
      responses:
        "201":
          description: Folder created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FolderDetail"
  /folders/{folderId}:
    get:
      operationId: getFolder
      tags: [folders]
      parameters:
        - $ref: "#/components/parameters/FolderId"
      responses:
        "200":
          description: Folder detail and ordered root-to-parent ancestors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FolderDetail"
    patch:
      operationId: updateFolder
      tags: [folders]
      parameters:
        - $ref: "#/components/parameters/FolderId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FolderUpdate"
      responses:
        "200":
          description: Updated folder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FolderDetail"
    delete:
      operationId: deleteFolder
      tags: [folders]
      parameters:
        - $ref: "#/components/parameters/FolderId"
      responses:
        "204":
          description: Empty folder deleted
  /videos/upload-init:
    post:
      operationId: initializeVideoUpload
      tags: [videos]
      parameters:
        - in: header
          name: Idempotency-Key
          required: true
          schema:
            type: string
            minLength: 16
            maxLength: 128
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VideoUploadInput"
      responses:
        "201":
          description: Direct-upload credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VideoUploadInitialization"
  /videos/{videoId}:
    get:
      operationId: getVideo
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "200":
          description: Video detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Video"
    patch:
      operationId: updateVideo
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VideoUpdate"
      responses:
        "200":
          description: Updated video
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Video"
    delete:
      operationId: deleteVideo
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "204":
          description: Video deleted
  /videos/{videoId}/playback:
    get:
      operationId: getAuthenticatedVideoPlayback
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "200":
          description: Tenant-scoped provider-neutral playback metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthenticatedPlaybackVideo"
  /videos/{videoId}/master-status:
    get:
      operationId: getMasterStorageStatus
      tags: [videos]
      parameters: [ { $ref: "#/components/parameters/VideoId" } ]
      responses:
        "200":
          description: Tenant-scoped master archive status without provider or storage identity.
          content: { application/json: { schema: { $ref: "#/components/schemas/MasterStorageStatus" } } }
        "404": { description: Video not found }
  /videos/{videoId}/master-archive:
    post:
      operationId: archiveVideoMaster
      tags: [videos]
      parameters: [ { $ref: "#/components/parameters/VideoId" } ]
      responses:
        "202":
          description: Durable archive operation accepted.
          content: { application/json: { schema: { $ref: "#/components/schemas/MasterStorageOperation" } } }
        "409": { description: Video is not eligible for archive }
        "503": { description: Master archive adapters are not configured }
  /videos/{videoId}/master-restore:
    post:
      operationId: restoreVideoMaster
      tags: [videos]
      parameters: [ { $ref: "#/components/parameters/VideoId" } ]
      responses:
        "202":
          description: Durable restore operation accepted.
          content: { application/json: { schema: { $ref: "#/components/schemas/MasterStorageOperation" } } }
        "409": { description: Video has no archived master or is busy }
        "503": { description: Master archive adapters are not configured }
  /videos/{videoId}/thumbnail-upload-intent:
    post:
      operationId: createThumbnailUploadIntent
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ThumbnailUploadIntentInput"
      responses:
        "201":
          description: Short-lived exact-object upload capability
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThumbnailUploadIntent"
  /videos/{videoId}/thumbnail-finalize:
    post:
      operationId: finalizeThumbnail
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ThumbnailFinalizeInput"
      responses:
        "200":
          description: Finalized thumbnail application route
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Thumbnail"
  /videos/{videoId}/thumbnail:
    get:
      operationId: getVideoThumbnail
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      description: Requires the opaque current `v` query value returned in thumbnailUrl.
      responses:
        "200":
          description: Authenticated thumbnail bytes
          content:
            image/jpeg: {}
            image/png: {}
            image/webp: {}
    delete:
      operationId: deleteVideoThumbnail
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "204":
          description: Thumbnail removed
  /videos/{videoId}/playback/source:
    get:
      operationId: getAuthenticatedVideoPlaybackSource
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "307":
          description: Authenticated redirect to a short-lived signed playback manifest
          headers:
            Location:
              schema: { type: string }
  /videos/{videoId}/upload-complete:
    post:
      operationId: completeVideoUpload
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "200":
          description: Upload acknowledged for provider processing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Video"
  /videos/{videoId}/upload-cancel:
    post:
      operationId: cancelVideoUpload
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "200":
          description: Upload cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Video"
  /activity:
    get:
      operationId: listActivity
      tags: [dashboard]
      responses:
        "200":
          description: Recent workspace activity
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ActivityItem"
  /audit-events:
    get:
      operationId: listAuditEvents
      tags: [audit]
      parameters:
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
        - { name: cursor, in: query, schema: { type: string } }
        - { name: category, in: query, schema: { type: string } }
        - { name: action, in: query, schema: { type: string } }
        - { name: subjectType, in: query, schema: { type: string } }
        - { name: subjectId, in: query, schema: { type: string } }
        - { name: actorKind, in: query, schema: { type: string, enum: [user, system, webhook, job] } }
        - { name: actorUserId, in: query, schema: { type: string } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: search, in: query, schema: { type: string, maxLength: 200 } }
      responses:
        "200":
          description: Stable, tenant-scoped audit event page
          content: { application/json: { schema: { $ref: "#/components/schemas/AuditEventPage" } } }
  /audit-events/export:
    get:
      operationId: exportAuditEvents
      tags: [audit]
      parameters:
        - { name: category, in: query, schema: { type: string } }
        - { name: action, in: query, schema: { type: string } }
        - { name: subjectType, in: query, schema: { type: string } }
        - { name: subjectId, in: query, schema: { type: string } }
        - { name: actorKind, in: query, schema: { type: string, enum: [user, system, webhook, job] } }
        - { name: actorUserId, in: query, schema: { type: string } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
        - { name: search, in: query, schema: { type: string, maxLength: 200 } }
      responses:
        "200":
          description: UTF-8 CSV audit export, capped at 10,000 rows
          content: { text/csv: { schema: { type: string, format: binary } } }
  /permission-groups:
    get:
      operationId: listPermissionGroups
      tags: [members]
      responses:
        "200":
          description: Permission groups available in the current workspace
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PermissionGroup"
    post:
      operationId: createPermissionGroup
      tags: [members]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PermissionGroupInput"
      responses:
        "201":
          description: Permission group created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PermissionGroup"
  /workspaces:
    get:
      operationId: listWorkspaces
      tags: [workspace]
      responses:
        "200":
          description: Active workspaces available to the signed-in user
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/WorkspaceSelector" }
  /workspaces/select:
    post:
      operationId: selectWorkspace
      tags: [workspace]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/WorkspaceSelectionInput" }
      responses:
        "200":
          description: Workspace selected for subsequent tenant-scoped requests
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WorkspaceSelector" }
  /permission-groups/{groupId}:
    patch:
      operationId: updatePermissionGroup
      tags: [members]
      parameters:
        - $ref: "#/components/parameters/GroupId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PermissionGroupInput"
      responses:
        "200":
          description: Permission group updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PermissionGroup"
    delete:
      operationId: deletePermissionGroup
      tags: [members]
      parameters:
        - $ref: "#/components/parameters/GroupId"
      responses:
        "204":
          description: Permission group deleted
  /permissions:
    get:
      operationId: listPermissions
      tags: [members]
      responses:
        "200":
          description: Permission catalog
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Permission"
  /members:
    get:
      operationId: listMembers
      tags: [members]
      responses:
        "200":
          description: Current workspace members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Member"
  /members/{membershipId}:
    patch:
      operationId: updateMember
      tags: [members]
      parameters:
        - $ref: "#/components/parameters/MembershipId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MemberUpdate"
      responses:
        "200":
          description: Member updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Member"
  /invitations:
    post:
      operationId: createInvitation
      tags: [members]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvitationInput"
      responses:
        "201":
          description: Invitation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invitation"
  /invitations/accept:
    post:
      operationId: acceptInvitation
      tags: [members]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/InvitationAcceptanceInput" }
      responses:
        "200":
          description: Invitation accepted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/InvitationAcceptance" }
  /invitations/{invitationId}:
    delete:
      operationId: revokeInvitation
      tags: [members]
      parameters: [{ $ref: "#/components/parameters/InvitationId" }]
      responses:
        "204": { description: Invitation revoked }
  /invitations/{invitationId}/reissue:
    post:
      operationId: reissueInvitation
      tags: [members]
      parameters: [{ $ref: "#/components/parameters/InvitationId" }]
      responses:
        "201":
          description: Invitation reissued
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Invitation" }
  /public/videos/{videoId}:
    get:
      operationId: getPublicVideo
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "200":
          description: Provider-neutral public playback metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicVideo"
  /public/videos/{videoId}/source:
    get:
      operationId: getPublicVideoSource
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      responses:
        "307":
          description: Redirect to a short-lived signed playback manifest
          headers:
            Location:
              schema: { type: string }
  /public/videos/{videoId}/thumbnail:
    get:
      operationId: getPublicVideoThumbnail
      tags: [videos]
      parameters:
        - $ref: "#/components/parameters/VideoId"
      description: Requires the opaque current `v` query value returned in thumbnailUrl.
      responses:
        "200":
          description: Public or unlisted thumbnail bytes
          content:
            image/jpeg: {}
            image/png: {}
            image/webp: {}
  /public/playback-events:
    post:
      operationId: createPlaybackEvents
      tags: [videos]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlaybackEventBatch"
      responses:
        "202":
          description: Events accepted
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [accepted, duplicates]
                properties:
                  accepted: { type: number }
                  duplicates: { type: number }
        "400": { description: Invalid event batch }
        "401": { description: "Missing, malformed, tampered, or expired analytics grant" }
        "403": { description: Grant is not valid for the active public embed }
        "409": { description: Event identifier collision }
        "429":
          description: Analytics ingestion rate limit exceeded
          headers:
            Retry-After:
              schema: { type: integer }
components:
  parameters:
    VideoId:
      in: path
      name: videoId
      required: true
      schema:
        type: string
        format: uuid
    FolderId:
      in: path
      name: folderId
      required: true
      schema:
        type: string
    GroupId:
      in: path
      name: groupId
      required: true
      schema:
        type: string
    MembershipId:
      in: path
      name: membershipId
      required: true
      schema:
        type: string
    InvitationId:
      in: path
      name: invitationId
      required: true
      schema:
        type: string
  schemas:
    MasterStorageOperation:
      type: object
      additionalProperties: false
      required: [operation, state, diagnosticCode, retryable, createdAt, updatedAt, completedAt]
      properties:
        operation: { type: string, enum: [archive, restore] }
        state: { type: string, enum: [pending, dispatching, queued, processing, completed, failed, reconciliation_required, cancelled] }
        diagnosticCode: { type: string, nullable: true }
        retryable: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        completedAt: { type: string, format: date-time, nullable: true }
    MasterStorageStatus:
      type: object
      additionalProperties: false
      required: [storageConfigured, sourceTransferConfigured, hasArchivedMaster, archivedAt, latestArchiveOperation, latestRestoreOperation]
      properties:
        storageConfigured: { type: boolean }
        sourceTransferConfigured: { type: boolean }
        hasArchivedMaster: { type: boolean }
        archivedAt: { type: string, format: date-time, nullable: true }
        latestArchiveOperation: { allOf: [ { $ref: "#/components/schemas/MasterStorageOperation" } ], nullable: true }
        latestRestoreOperation: { allOf: [ { $ref: "#/components/schemas/MasterStorageOperation" } ], nullable: true }
    WorkspaceOnboardingInput:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: { type: string, minLength: 2, maxLength: 100 }
        slug:
          type: string
          minLength: 2
          maxLength: 63
          description: Human-entered slug normalized by the server to lower kebab case.
    WorkspaceOnboardingRef:
      type: object
      additionalProperties: false
      properties:
        workspaceId: { type: string }
    WorkspaceOnboardingWorkspace:
      type: object
      additionalProperties: false
      required: [id, name, slug, status]
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        status: { type: string, enum: [provisioning, active, failed, suspended] }
    WorkspaceProvisioningSummary:
      type: object
      additionalProperties: false
      required: [state, retryable, message]
      properties:
        state: { type: string, enum: [pending, creating, ready, reconciliation_required, unavailable, failed] }
        retryable: { type: boolean }
        message: { type: string }
    WorkspaceOnboarding:
      type: object
      additionalProperties: false
      required: [state, provisioning]
      properties:
        state: { type: string, enum: [needs_workspace, provisioning, active, failed, suspended] }
        workspace: { $ref: "#/components/schemas/WorkspaceOnboardingWorkspace" }
        provisioning: { $ref: "#/components/schemas/WorkspaceProvisioningSummary" }
    BillingPrice:
      type: object
      required: [interval, amount, currency]
      properties:
        interval: { type: string, enum: [month, year] }
        amount: { type: number }
        currency: { type: string, enum: [usd] }
    BillingCatalogPlan:
      type: object
      required: [code, name, description, entitlements, prices]
      properties:
        code: { type: string, enum: [starter, growth, scale] }
        name: { type: string }
        description: { type: string }
        entitlements: { type: object, additionalProperties: true }
        prices: { type: array, minItems: 2, maxItems: 2, items: { $ref: "#/components/schemas/BillingPrice" } }
    BillingCatalog:
      type: object
      required: [plans]
      properties:
        plans: { type: array, items: { $ref: "#/components/schemas/BillingCatalogPlan" } }
    BillingPlanInput:
      type: object
      additionalProperties: false
      required: [plan, interval, idempotencyKey]
      properties:
        plan: { type: string, enum: [starter, growth, scale] }
        interval: { type: string, enum: [month, year] }
        idempotencyKey:
          type: string
          pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$"
    BillingActionInput:
      type: object
      additionalProperties: false
      required: [idempotencyKey]
      properties:
        idempotencyKey:
          type: string
          pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$"
    BillingUrl:
      type: object
      required: [url]
      properties: { url: { type: string } }
    BillingSubscription:
      type: object
      required: [status, cancelAtPeriodEnd, capabilities]
      properties:
        status: { type: string, enum: [unmanaged, incomplete, active, trialing, past_due, unpaid, canceled, restricted, quarantined] }
        plan: { type: ["string", "null"] }
        pendingPlan: { type: ["string", "null"] }
        pendingEffectiveAt: { type: ["string", "null"], format: date-time }
        interval: { type: ["string", "null"], enum: [month, year, null] }
        periodStart: { type: ["string", "null"], format: date-time }
        periodEnd: { type: ["string", "null"], format: date-time }
        graceEndsAt: { type: ["string", "null"], format: date-time }
        cancelAtPeriodEnd: { type: boolean }
        capabilities:
          type: object
          required: [canManage, canSubscribe]
          properties:
            canManage: { type: boolean }
            canSubscribe: { type: boolean }
    BillingChangeResult:
      type: object
      required: [scheduled, effectiveAt, reference]
      properties:
        scheduled: { type: boolean }
        effectiveAt: { type: ["string", "null"], format: date-time }
        reference: { type: string }
    BillingCancellation:
      type: object
      required: [cancelAtPeriodEnd]
      properties: { cancelAtPeriodEnd: { type: boolean } }
    BillingInvoice:
      type: object
      required: [id, createdAt, amountDue, amountPaid, currency]
      properties:
        id: { type: string }
        status: { type: ["string", "null"] }
        createdAt: { type: string, format: date-time }
        amountDue: { type: number }
        amountPaid: { type: number }
        currency: { type: string }
        hostedInvoiceUrl: { type: ["string", "null"] }
        invoicePdf: { type: ["string", "null"] }
    BillingInvoiceList:
      type: object
      required: [items, nextCursor]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/BillingInvoice" } }
        nextCursor: { type: ["string", "null"] }
    BillingReconcileResult:
      type: object
      required: [status, reconciledAt]
      properties:
        status: { type: string }
        reconciledAt: { type: string, format: date-time }
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string
    RuntimeConfig:
      type: object
      required: [productName, appDomain]
      properties:
        productName: { type: string }
        appDomain: { type: string }
    Workspace:
      type: object
      required: [id, name, slug, plan, memberCount, storageUsedGb, storageLimitGb, entitlements, billingAccess, permissions, playerAccent, playerControlForeground, playerControlBackground, logoInitials, logoObjectKey, watermarkObjectKey, posterTreatment, customDomain, customDomainVerified]
      properties:
        id: { type: string }
        name: { type: string }
        slug: { type: string }
        plan: { type: string }
        memberCount: { type: number }
        storageUsedGb: { type: number }
        storageLimitGb: { type: number }
        entitlements:
          type: object
          additionalProperties:
            oneOf:
              - { type: boolean }
              - { type: number }
              - { type: string }
        billingAccess:
          type: object
          required: [status, canCreate, graceEndsAt]
          properties:
            status: { type: string }
            canCreate: { type: boolean }
            graceEndsAt: { type: ["string", "null"], format: date-time }
        permissions:
          type: array
          items: { type: string }
        playerAccent: { type: string }
        playerControlForeground: { type: string }
        playerControlBackground: { type: string }
        logoInitials: { type: string }
        logoObjectKey: { type: ["string", "null"] }
        watermarkObjectKey: { type: ["string", "null"] }
        posterTreatment:
          type: string
          enum: [default, darken, gradient]
        customDomain: { type: ["string", "null"] }
        customDomainVerified: { type: boolean }
    WorkspaceSelector:
      type: object
      additionalProperties: false
      required: [id, name, slug, current]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        current: { type: boolean }
    WorkspaceSelectionInput:
      type: object
      additionalProperties: false
      oneOf:
        - required: [id]
        - required: [slug]
      properties:
        id: { type: string, format: uuid }
        slug:
          type: string
          minLength: 2
          maxLength: 63
          pattern: "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$"
    WorkspaceUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        playerAccent: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        playerControlForeground: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        playerControlBackground: { type: string, pattern: "^#[0-9A-Fa-f]{6}$" }
        logoInitials: { type: string, minLength: 1, maxLength: 3 }
        logoObjectKey: { type: ["string", "null"], maxLength: 1024 }
        watermarkObjectKey: { type: ["string", "null"], maxLength: 1024 }
        posterTreatment:
          type: string
          enum: [default, darken, gradient]
    CustomDomainInput:
      type: object
      additionalProperties: false
      required: [hostname]
      properties:
        hostname: { type: string, minLength: 1, maxLength: 253 }
    CustomDomainStatus:
      type: object
      additionalProperties: false
      required: [hostname, lifecycleState, txtRecordName, txtRecordValue, lastCheckedAt, verifiedAt, retryable, message, activationState]
      properties:
        hostname: { type: ["string", "null"] }
        lifecycleState:
          type: ["string", "null"]
          enum: [pending_verification, verifying, verified, failed, suspended, removed, reconciliation_required, null]
        txtRecordName: { type: ["string", "null"] }
        txtRecordValue: { type: ["string", "null"] }
        lastCheckedAt: { type: ["string", "null"], format: date-time }
        verifiedAt: { type: ["string", "null"], format: date-time }
        retryable: { type: boolean }
        message: { type: string }
        activationState:
          type: string
          enum: [not_ready, external_setup_required]
    Dashboard:
      type: object
      required: [totalVideos, totalPlays, watchTimeHours, completionRate, playsTrend, topVideos]
      properties:
        totalVideos: { type: number }
        totalPlays: { type: number }
        watchTimeHours: { type: number }
        completionRate: { type: number }
        playsTrend:
          type: array
          items:
            $ref: "#/components/schemas/TrendPoint"
        topVideos:
          type: array
          items:
            $ref: "#/components/schemas/TopVideo"
    TrendPoint:
      type: object
      required: [date, plays]
      properties:
        date: { type: string }
        plays: { type: number }
    TopVideo:
      type: object
      required: [id, title, plays, completionRate]
      properties:
        id: { type: string }
        title: { type: string }
        plays: { type: number }
        completionRate: { type: number }
    Video:
      type: object
      required: [id, title, description, status, visibility, durationSeconds, createdAt, thumbnailColor, plays, completionRate, folderId, folderName, folderPath]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        status:
          type: string
          enum: [created, uploading, processing, ready, error]
        visibility:
          type: string
          enum: [private, unlisted, public]
        durationSeconds: { type: number }
        createdAt: { type: string, format: date-time }
        thumbnailColor: { type: string }
        thumbnailUrl: { type: ["string", "null"] }
        plays: { type: number }
        completionRate: { type: number }
        folderId: { type: ["string", "null"] }
        folderName: { type: ["string", "null"] }
        folderPath:
          type: array
          items:
            $ref: "#/components/schemas/FolderAncestor"
        embedUrl: { type: string }
        embedPath: { type: string }
        embedCode: { type: string }
        videoObject:
          $ref: "#/components/schemas/VideoObject"
    VideoList:
      type: object
      required: [items, nextCursor, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Video"
        nextCursor:
          type: ["string", "null"]
        total:
          type: number
          minimum: 0
    VideoObject:
      type: object
      required: ["@context", "@type", name, description, embedUrl, duration]
      properties:
        "@context": { type: string, enum: ["https://schema.org"] }
        "@type": { type: string, enum: [VideoObject] }
        name: { type: string }
        description: { type: string }
        embedUrl: { type: string }
        duration: { type: string }
        thumbnailUrl: { type: string }
    VideoUploadInput:
      type: object
      required: [title, fileName, contentType, contentLength]
      properties:
        title: { type: string, minLength: 1 }
        description: { type: string }
        fileName: { type: string, minLength: 1, maxLength: 255 }
        contentType: { type: string, minLength: 1, maxLength: 127 }
        contentLength: { type: number, minimum: 1, maximum: 2147483647 }
        folderId: { type: ["string", "null"] }
    ThumbnailUploadIntentInput:
      type: object
      additionalProperties: false
      required: [contentType, sizeBytes]
      properties:
        contentType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
        sizeBytes: { type: number, minimum: 1, maximum: 10485760 }
    ThumbnailUploadIntent:
      type: object
      required: [intentId, uploadUrl, requiredHeaders, expiresAt]
      properties:
        intentId: { type: string }
        uploadUrl: { type: string }
        requiredHeaders:
          type: object
          additionalProperties: { type: string }
        expiresAt: { type: string, format: date-time }
    ThumbnailFinalizeInput:
      type: object
      additionalProperties: false
      required: [intentId]
      properties:
        intentId: { type: string }
    Thumbnail:
      type: object
      required: [thumbnailUrl, contentType, sizeBytes]
      properties:
        thumbnailUrl:
          type: string
          description: Versioned owned application URL. The object key and storage generation are never exposed.
        contentType:
          type: string
          enum: [image/jpeg, image/png, image/webp]
        sizeBytes: { type: number, minimum: 1, maximum: 10485760 }
    VideoUploadInitialization:
      type: object
      required: [videoId, upload]
      properties:
        videoId: { type: string }
        upload:
          oneOf:
            - $ref: "#/components/schemas/TusUploadCredentials"
            - $ref: "#/components/schemas/MultipartUploadCredentials"
    TusUploadCredentials:
      type: object
      required: [kind, endpoint, headers, expiresAt]
      properties:
        kind: { type: string, enum: [tus] }
        endpoint: { type: string }
        headers:
          type: object
          additionalProperties: { type: string }
        expiresAt: { type: string, format: date-time }
    MultipartUploadCredentials:
      type: object
      required: [kind, uploadId, partSizeBytes, parts, completeUrl, expiresAt]
      properties:
        kind: { type: string, enum: [multipart] }
        uploadId: { type: string }
        partSizeBytes: { type: number }
        parts:
          type: array
          items:
            type: object
            required: [number, url, headers]
            properties:
              number: { type: number }
              url: { type: string }
              headers: { type: object, additionalProperties: { type: string } }
        completeUrl: { type: string }
        expiresAt: { type: string, format: date-time }
    VideoUpdate:
      type: object
      properties:
        title: { type: string, minLength: 1 }
        description: { type: string }
        visibility:
          type: string
          enum: [private, unlisted, public]
        folderId: { type: ["string", "null"] }
    BulkVideoUpdate:
      oneOf:
        - $ref: "#/components/schemas/BulkVideoMoveInput"
        - $ref: "#/components/schemas/BulkVideoVisibilityInput"
    BulkVideoMoveInput:
      type: object
      required: [operation, videoIds, folderId]
      additionalProperties: false
      properties:
        operation: { type: string, enum: [move] }
        videoIds:
          type: array
          minItems: 1
          maxItems: 50
          uniqueItems: true
          items: { type: string, pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" }
        folderId:
          type: ["string", "null"]
          pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$"
    BulkVideoVisibilityInput:
      type: object
      required: [operation, videoIds, visibility]
      additionalProperties: false
      properties:
        operation: { type: string, enum: [visibility] }
        videoIds:
          type: array
          minItems: 1
          maxItems: 50
          uniqueItems: true
          items: { type: string, pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" }
        visibility:
          type: string
          enum: [private, unlisted, public]
    BulkVideoDeleteInput:
      type: object
      required: [videoIds]
      additionalProperties: false
      properties:
        videoIds:
          type: array
          minItems: 1
          maxItems: 25
          uniqueItems: true
          items: { type: string, pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" }
    BulkVideoActionFailure:
      type: object
      required: [videoId, status, error]
      properties:
        videoId: { type: string, pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" }
        status: { type: integer, enum: [404, 409, 503] }
        error: { type: string }
    BulkVideoActionResult:
      type: object
      required: [succeeded, failed]
      properties:
        succeeded:
          type: array
          items: { type: string, pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" }
        failed:
          type: array
          items:
            $ref: "#/components/schemas/BulkVideoActionFailure"
    FolderAncestor:
      type: object
      required: [id, name]
      properties:
        id: { type: string }
        name: { type: string }
    Folder:
      type: object
      required: [id, parentId, name, childFolderCount, videoCount, createdAt, updatedAt]
      properties:
        id: { type: string }
        parentId: { type: ["string", "null"] }
        name: { type: string }
        childFolderCount: { type: number, minimum: 0 }
        videoCount: { type: number, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    FolderDetail:
      type: object
      required: [id, parentId, name, childFolderCount, videoCount, createdAt, updatedAt, ancestors]
      properties:
        id: { type: string }
        parentId: { type: ["string", "null"] }
        name: { type: string }
        childFolderCount: { type: number, minimum: 0 }
        videoCount: { type: number, minimum: 0 }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        ancestors:
          type: array
          items:
            $ref: "#/components/schemas/FolderAncestor"
    FolderInput:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        parentId: { type: ["string", "null"] }
    FolderUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        parentId: { type: ["string", "null"] }
    ActivityItem:
      type: object
      required: [id, action, subject, actor, createdAt]
      properties:
        id: { type: string }
        action: { type: string }
        subject: { type: string }
        actor: { type: string }
        createdAt: { type: string, format: date-time }
    AuditEventPage:
      type: object
      required: [items, nextCursor, snapshotAt]
      properties:
        items: { type: array, items: { $ref: "#/components/schemas/AuditEvent" } }
        nextCursor: { type: [string, "null"] }
        snapshotAt: { type: string, format: date-time }
    AuditEvent:
      type: object
      required: [id, action, category, actor, subject, metadata, createdAt]
      properties:
        id: { type: string }
        action: { type: string }
        category: { type: string }
        actor:
          type: object
          required: [kind, userId, name]
          properties:
            kind: { type: string, enum: [user, system, webhook, job] }
            userId: { type: [string, "null"] }
            name: { type: string }
        subject:
          type: object
          required: [type, id, label]
          properties:
            type: { type: string }
            id: { type: [string, "null"] }
            label: { type: string }
        beforeState: { type: [object, "null"], additionalProperties: true }
        afterState: { type: [object, "null"], additionalProperties: true }
        metadata: { type: object, additionalProperties: true }
        requestId: { type: [string, "null"] }
        createdAt: { type: string, format: date-time }
    PermissionGroup:
      type: object
      required: [id, name, description, systemKey, permissions]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        systemKey: { type: ["string", "null"] }
        permissions:
          type: array
          items: { type: string }
    PermissionGroupInput:
      type: object
      required: [name, description, permissions]
      properties:
        name: { type: string, minLength: 1, maxLength: 80 }
        description: { type: string, maxLength: 240 }
        permissions:
          type: array
          uniqueItems: true
          items: { type: string }
    Permission:
      type: object
      required: [key, description]
      properties:
        key: { type: string }
        description: { type: string }
    Member:
      type: object
      required: [id, name, email, groupId, role, status]
      properties:
        id: { type: string }
        name: { type: string }
        email: { type: string, pattern: "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$" }
        groupId: { type: string }
        role: { type: string }
        status:
          type: string
          enum: [invited, active, suspended]
    MemberUpdate:
      type: object
      properties:
        groupId: { type: string }
        status:
          type: string
          enum: [active, suspended]
    InvitationInput:
      type: object
      required: [email, groupId]
      properties:
        email: { type: string, pattern: "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$" }
        groupId: { type: string }
    InvitationAcceptanceInput:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token: { type: string, minLength: 43, maxLength: 43, pattern: "^[A-Za-z0-9_-]+$" }
    InvitationAcceptance:
      type: object
      required: [accepted]
      properties:
        accepted: { type: boolean }
    Invitation:
      type: object
      required: [id, email, groupId, role, status, expiresAt]
      properties:
        id: { type: string }
        email: { type: string, pattern: "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$" }
        groupId: { type: string }
        role: { type: string }
        status:
          type: string
          enum: [invited]
        expiresAt: { type: string, format: date-time }
    PublicVideo:
      type: object
      additionalProperties: false
      required: [id, title, description, status, visibility, durationSeconds, thumbnailColor, playerAccent, playerControlForeground, playerControlBackground, posterTreatment]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        status:
          type: string
          enum: [created, uploading, processing, ready, error]
        visibility:
          type: string
          enum: [unlisted, public]
        durationSeconds: { type: number }
        thumbnailColor: { type: string }
        thumbnailUrl: { type: ["string", "null"] }
        playerAccent: { type: string }
        playerControlForeground: { type: string }
        playerControlBackground: { type: string }
        posterTreatment: { type: string }
        sourceUrl: { type: string }
        sourceType:
          type: string
          enum: [hls, native]
        sourceExpiresAt: { type: string, format: date-time }
        posterUrl:
          type: ["string", "null"]
        logoUrl: { type: string }
        watermarkUrl: { type: string }
        analyticsGrant: { type: string }
        analyticsGrantExpiresAt: { type: string, format: date-time }
    AuthenticatedPlaybackVideo:
      type: object
      required: [id, title, description, status, visibility, durationSeconds, thumbnailColor, playerAccent, playerControlForeground, playerControlBackground, posterTreatment]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        status:
          type: string
          enum: [created, uploading, processing, ready, error]
        visibility:
          type: string
          enum: [private, unlisted, public]
        durationSeconds: { type: number }
        thumbnailColor: { type: string }
        thumbnailUrl: { type: ["string", "null"] }
        playerAccent: { type: string }
        playerControlForeground: { type: string }
        playerControlBackground: { type: string }
        posterTreatment: { type: string }
        sourceUrl: { type: string }
        sourceType:
          type: string
          enum: [hls, native]
        sourceExpiresAt: { type: string, format: date-time }
        posterUrl:
          type: ["string", "null"]
        logoUrl: { type: string }
        watermarkUrl: { type: string }
    PlaybackEventInput:
      type: object
      additionalProperties: false
      required: [eventId, sessionId, type, occurredAt]
      properties:
        eventId: { type: string, pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" }
        sessionId: { type: string, pattern: "^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}$" }
        type:
          type: string
          enum: [load, play, progress, pause, ended, error]
        positionSeconds: { type: number, minimum: 0, maximum: 86400 }
        durationSeconds: { type: number, minimum: 0, maximum: 86400 }
        occurredAt: { type: string, format: date-time }
        errorCategory:
          type: string
          enum: [network, media, decode, source, unknown]
    PlaybackEventBatch:
      type: object
      additionalProperties: false
      required: [grant, events]
      properties:
        grant: { type: string, minLength: 20, maxLength: 4096 }
        events:
          type: array
          minItems: 1
          maxItems: 50
          items:
            $ref: "#/components/schemas/PlaybackEventInput"