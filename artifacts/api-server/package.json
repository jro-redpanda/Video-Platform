{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "export NODE_ENV=development && pnpm run build && pnpm run start",
    "build": "node ./build.mjs",
    "start": "node --enable-source-maps ./dist/index.mjs",
    "queue:smoke": "test -n \"$SESSION_SECRET\" || { echo \"SESSION_SECRET is required\" >&2; exit 1; }; pnpm run build && node ./dist/queue-smoke.mjs",
    "provider:smoke": "node ./scripts/provider-smoke.mjs",
    "upload:smoke": "NODE_ENV=test pnpm run build && NODE_ENV=test node ./dist/upload-step9-smoke.mjs",
    "webhook:smoke": "NODE_ENV=test pnpm run build && NODE_ENV=test node ./dist/webhook-step10-smoke.mjs",
    "embed:smoke": "NODE_ENV=test pnpm run build && NODE_ENV=test node ./dist/embed-step11-smoke.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.7.2",
    "@workspace/api-zod": "workspace:*",
    "@workspace/db": "workspace:*",
    "@workspace/providers": "workspace:*",
    "better-auth": "^1.7.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "drizzle-orm": "catalog:",
    "express": "^5.2.1",
    "pg-boss": "^12.29.0",
    "pino": "^9.14.0",
    "pino-http": "^10.5.0"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "catalog:",
    "esbuild": "0.27.3",
    "esbuild-plugin-pino": "^2.3.3",
    "pino-pretty": "^13.1.3",
    "thread-stream": "3.1.0"
  }
}
